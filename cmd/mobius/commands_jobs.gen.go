// Code generated by mobius-cligen. DO NOT EDIT.
//
// Regenerate with:  make generate-go-cli
//
// To suppress or override a command, edit
// cmd/mobius-cligen/overrides.go — never hand-edit this file.

package main

import (
	"fmt"

	"github.com/deepnoodle-ai/wonton/cli"

	"github.com/deepnoodle-ai/mobius/mobius/api"
)

// registerJobsCommands registers every generated subcommand in the "jobs" group.
func registerJobsCommands(app *cli.App) {
	jobsGrp := app.Group("jobs").Description("Worker runtime — claim, heartbeat, complete")
	jobsGrp.Alias("job")
	jobsGrp.Command("claim").
		Description("Long-poll for the next claimable workflow job").
		Flags(
			cli.Strings("actions", "").Help("Action names this worker can execute. When provided, only jobs whose `action` is in this list are r…"),
			cli.Int("concurrency-limit", "").Help("[required] Maximum number of concurrent in-flight jobs this worker process intends to hold. Recorded on the wo…"),
			cli.Strings("queues", "").Help("Queue names the worker subscribes to. When empty the worker claims from any queue in the project. W…"),
			cli.Int("wait-seconds", "").Help("How long to hold the request open waiting for a job to surface. 0 returns immediately. Capped at 30…"),
			cli.String("worker-instance-id", "").Help("[required] Caller-configured stable identifier for the worker process. The SDK auto-detects this on Cloud Run,…"),
			cli.String("worker-name", "").Help("Optional human-readable name shown in the worker list."),
			cli.String("worker-session-token", "").Help("[required] Per-boot token generated by the SDK on Worker construction. Stamped onto the claimed job and re-pre…"),
			cli.String("worker-version", "").Help("Optional version string shown in the worker list."),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			var body api.ClaimJobJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("actions") {
				v := ctx.Strings("actions")
				body.Actions = &v
			}
			if ctx.IsSet("concurrency-limit") {
				body.ConcurrencyLimit = ctx.Int("concurrency-limit")
			}
			if ctx.IsSet("queues") {
				v := ctx.Strings("queues")
				body.Queues = &v
			}
			if ctx.IsSet("wait-seconds") {
				v := ctx.Int("wait-seconds")
				body.WaitSeconds = &v
			}
			if ctx.IsSet("worker-instance-id") {
				body.WorkerInstanceId = ctx.String("worker-instance-id")
			}
			if ctx.IsSet("worker-name") {
				v := ctx.String("worker-name")
				body.WorkerName = &v
			}
			if ctx.IsSet("worker-session-token") {
				body.WorkerSessionToken = ctx.String("worker-session-token")
			}
			if ctx.IsSet("worker-version") {
				v := ctx.String("worker-version")
				body.WorkerVersion = &v
			}
			if body.ConcurrencyLimit == 0 {
				return fmt.Errorf("--concurrency-limit is required (or supply it via --file)")
			}
			if body.WorkerInstanceId == "" {
				return fmt.Errorf("--worker-instance-id is required (or supply it via --file)")
			}
			if body.WorkerSessionToken == "" {
				return fmt.Errorf("--worker-session-token is required (or supply it via --file)")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.ClaimJobWithResponse(ctx.Context(), p0, body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "claimJob", resp.StatusCode(), resp.Body)
		})

	jobsGrp.Command("complete-job").
		Description("Report terminal status for a claimed workflow job").
		Args("id").
		Flags(
			cli.Int("attempt", "").Help("[required] Must match the `attempt` from the original claim."),
			cli.String("error-message", "").Help("Human-readable error detail. Only relevant for failed status."),
			cli.String("error-type", "").Help("Short error class identifier (e.g. \"TimeoutError\"). Used for observability and retry classification…"),
			cli.String("result-b64", "").Help("Base64-encoded (standard encoding) bytes representing the job's output. The workflow engine decodes…"),
			cli.String("status", "").Help("[required] Terminal status for this job attempt: `completed` or `failed`. `failed` triggers the workflow engin…"),
			cli.String("worker-instance-id", "").Help("Echo of the claim's `worker_instance_id`. Audit only."),
			cli.String("worker-session-token", "").Help("[required] Per-boot token from the claim. Compared against the job's `claimed_by_session_token`; mismatch retu…"),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			p1 := ctx.Arg(0)
			var body api.CompleteJobJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("attempt") {
				body.Attempt = ctx.Int("attempt")
			}
			if ctx.IsSet("error-message") {
				v := ctx.String("error-message")
				body.ErrorMessage = &v
			}
			if ctx.IsSet("error-type") {
				v := ctx.String("error-type")
				body.ErrorType = &v
			}
			if ctx.IsSet("result-b64") {
				v := ctx.String("result-b64")
				body.ResultB64 = &v
			}
			if ctx.IsSet("status") {
				body.Status = api.JobCompleteRequestStatus(ctx.String("status"))
			}
			if ctx.IsSet("worker-instance-id") {
				v := ctx.String("worker-instance-id")
				body.WorkerInstanceId = &v
			}
			if ctx.IsSet("worker-session-token") {
				body.WorkerSessionToken = ctx.String("worker-session-token")
			}
			if body.Attempt == 0 {
				return fmt.Errorf("--attempt is required (or supply it via --file)")
			}
			if body.Status == "" {
				return fmt.Errorf("--status is required (or supply it via --file)")
			}
			if body.WorkerSessionToken == "" {
				return fmt.Errorf("--worker-session-token is required (or supply it via --file)")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.CompleteJobWithResponse(ctx.Context(), p0, p1, body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "completeJob", resp.StatusCode(), resp.Body)
		})

	jobsGrp.Command("create-interaction").
		Description("Create a human-in-the-loop interaction from a claimed workflow job").
		Args("id").
		Flags(
			cli.String("context", "").Help("Additional key-value context surfaced in the UI alongside the message. Accepts JSON, @file, or @-."),
			cli.String("expires-at", "").Help("Timestamp after which this interaction expires. Accepts JSON, @file, or @-."),
			cli.String("message", "").Help("[required] Message shown to the responder."),
			cli.Bool("require-all", "").Help("When target.type is \"group\", setting require_all=true means all snapshotted group members must resp…"),
			cli.String("resolution-policy", "").Help("Declarative resolution rule attached to an Interaction. Determines how participant responses become… Accepts JSON, @file, or @-."),
			cli.String("signal-name", "").Help("Optional signal name override. When omitted, the server derives the signal name from step_name or f…"),
			cli.String("spec", "").Help("Declarative dialog contract for rendering and validating an interaction. Used at both authoring tim… Accepts JSON, @file, or @-."),
			cli.String("step-name", "").Help("Optional workflow step label for UI/debugging context"),
			cli.String("target", "").Help("[required] Identifies who should receive an interaction request. Note: distinct from the caller/audit `Actor` … Accepts JSON, @file, or @-."),
			cli.String("timeout", "").Help("Optional duration string (e.g. \"24h\", \"30m\") specifying how long the interaction should remain open…"),
			cli.String("type", "").Help("[required] Interaction kind: `approval` captures a decision, `review` captures acknowledgement or notes, and `…"),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			p1 := ctx.Arg(0)
			var body api.CreateJobInteractionJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("context") {
				if err := decodeFlagJSON(ctx, "context", ctx.String("context"), &body.Context); err != nil {
					return err
				}
			}
			if ctx.IsSet("expires-at") {
				if err := decodeFlagJSON(ctx, "expires-at", ctx.String("expires-at"), &body.ExpiresAt); err != nil {
					return err
				}
			}
			if ctx.IsSet("message") {
				body.Message = ctx.String("message")
			}
			if ctx.IsSet("require-all") {
				v := ctx.Bool("require-all")
				body.RequireAll = &v
			}
			if ctx.IsSet("resolution-policy") {
				if err := decodeFlagJSON(ctx, "resolution-policy", ctx.String("resolution-policy"), &body.ResolutionPolicy); err != nil {
					return err
				}
			}
			if ctx.IsSet("signal-name") {
				v := ctx.String("signal-name")
				body.SignalName = &v
			}
			if ctx.IsSet("spec") {
				if err := decodeFlagJSON(ctx, "spec", ctx.String("spec"), &body.Spec); err != nil {
					return err
				}
			}
			if ctx.IsSet("step-name") {
				v := ctx.String("step-name")
				body.StepName = &v
			}
			if ctx.IsSet("target") {
				if err := decodeFlagJSON(ctx, "target", ctx.String("target"), &body.Target); err != nil {
					return err
				}
			}
			if ctx.IsSet("timeout") {
				v := ctx.String("timeout")
				body.Timeout = &v
			}
			if ctx.IsSet("type") {
				body.Type = api.InteractionType(ctx.String("type"))
			}
			if body.Message == "" {
				return fmt.Errorf("--message is required (or supply it via --file)")
			}
			if ctx.String("file") == "" && !ctx.IsSet("target") {
				return fmt.Errorf("--target is required (or supply it via --file)")
			}
			if body.Type == "" {
				return fmt.Errorf("--type is required (or supply it via --file)")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.CreateJobInteractionWithResponse(ctx.Context(), p0, p1, body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "createJobInteraction", resp.StatusCode(), resp.Body)
		})

	jobsGrp.Command("emit-job-events").
		Description("Emit one or more custom events from a claimed workflow job").
		Args("id").
		Flags(
			cli.Int("attempt", "").Help("[required] Must match the `attempt` from the original claim."),
			cli.String("events", "").Help("[required] The batch of events to publish. All events are validated atomically. Accepts JSON, @file, or @-."),
			cli.String("worker-instance-id", "").Help("Echo of the claim's `worker_instance_id`. Audit only."),
			cli.String("worker-session-token", "").Help("[required] Per-boot token from the claim. The lease fence value; mismatch against the job's `claimed_by_sessio…"),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			p1 := ctx.Arg(0)
			var body api.EmitJobEventsJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("attempt") {
				body.Attempt = ctx.Int("attempt")
			}
			if ctx.IsSet("events") {
				if err := decodeFlagJSON(ctx, "events", ctx.String("events"), &body.Events); err != nil {
					return err
				}
			}
			if ctx.IsSet("worker-instance-id") {
				v := ctx.String("worker-instance-id")
				body.WorkerInstanceId = &v
			}
			if ctx.IsSet("worker-session-token") {
				body.WorkerSessionToken = ctx.String("worker-session-token")
			}
			if body.Attempt == 0 {
				return fmt.Errorf("--attempt is required (or supply it via --file)")
			}
			if ctx.String("file") == "" && !ctx.IsSet("events") {
				return fmt.Errorf("--events is required (or supply it via --file)")
			}
			if body.WorkerSessionToken == "" {
				return fmt.Errorf("--worker-session-token is required (or supply it via --file)")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.EmitJobEventsWithResponse(ctx.Context(), p0, p1, body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "emitJobEvents", resp.StatusCode(), resp.Body)
		})

	jobsGrp.Command("get").
		Description("Get a workflow job by ID").
		Args("id").
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			p1 := ctx.Arg(0)
			resp, err := client.GetJobWithResponse(ctx.Context(), p0, p1)
			if err != nil {
				return err
			}
			return printResponse(ctx, "getJob", resp.StatusCode(), resp.Body)
		})

	jobsGrp.Command("heartbeat").
		Description("Refresh the lease on a claimed workflow job").
		Args("id").
		Flags(
			cli.Int("attempt", "").Help("[required] Must match the `attempt` from the original claim."),
			cli.String("worker-instance-id", "").Help("Echo of the `worker_instance_id` from the claim. Logged for audit; not the fence value."),
			cli.String("worker-session-token", "").Help("[required] Per-boot token from the original claim. Compared against the job's `claimed_by_session_token`; mism…"),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			p1 := ctx.Arg(0)
			var body api.HeartbeatJobJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("attempt") {
				body.Attempt = ctx.Int("attempt")
			}
			if ctx.IsSet("worker-instance-id") {
				v := ctx.String("worker-instance-id")
				body.WorkerInstanceId = &v
			}
			if ctx.IsSet("worker-session-token") {
				body.WorkerSessionToken = ctx.String("worker-session-token")
			}
			if body.Attempt == 0 {
				return fmt.Errorf("--attempt is required (or supply it via --file)")
			}
			if body.WorkerSessionToken == "" {
				return fmt.Errorf("--worker-session-token is required (or supply it via --file)")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.HeartbeatJobWithResponse(ctx.Context(), p0, p1, body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "heartbeatJob", resp.StatusCode(), resp.Body)
		})

	jobsGrp.Command("run-job-action").
		Description("Execute one server-side action for a claimed workflow job").
		Args("id", "action-name").
		Flags(
			cli.String("parameters", "").Help("Input parameters passed to the action handler. Accepts JSON, @file, or @-."),
			cli.Int("timeout-seconds", "").Help("Override the default action execution timeout."),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			p0 := authFor(ctx).Project
			p1 := ctx.Arg(0)
			p2 := ctx.Arg(1)
			var body api.RunJobActionJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("parameters") {
				if err := decodeFlagJSON(ctx, "parameters", ctx.String("parameters"), &body.Parameters); err != nil {
					return err
				}
			}
			if ctx.IsSet("timeout-seconds") {
				v := ctx.Int("timeout-seconds")
				body.TimeoutSeconds = &v
			}
			if ctx.String("file") == "" && !ctx.IsSet("parameters") && !ctx.IsSet("timeout-seconds") {
				return fmt.Errorf("at least one flag or --file is required")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.RunJobActionWithResponse(ctx.Context(), p0, p1, p2, body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "runJobAction", resp.StatusCode(), resp.Body)
		})

}
