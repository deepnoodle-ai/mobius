// Code generated by mobius-cligen. DO NOT EDIT.
//
// Regenerate with:  make generate-go-cli
//
// To suppress or override a command, edit
// cmd/mobius-cligen/overrides.go — never hand-edit this file.

package main

import (
	"fmt"

	"github.com/deepnoodle-ai/wonton/cli"

	"github.com/deepnoodle-ai/mobius/mobius/api"
)

// registerOrganizationsCommands registers every generated subcommand in the "organizations" group.
func registerOrganizationsCommands(app *cli.App) {
	organizationsGrp := app.Group("organizations").Description("Organization settings and control plane")
	organizationsGrp.Alias("organization")
	organizationsGrp.Command("get-definition-resolver").
		Description("Get the org's definition-resolver config").
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			resp, err := client.GetDefinitionResolverWithResponse(ctx.Context())
			if err != nil {
				return err
			}
			return printResponse(ctx, "getDefinitionResolver", resp.StatusCode(), resp.Body)
		})

	organizationsGrp.Command("replace-definition-resolver").
		Description("Replace the org's definition-resolver config").
		Flags(
			cli.String("auth", "").Help("Shared-secret auth for the client resolver. The token is write-only: supply it to set or rotate, send an empty string to clear. Accepts JSON, @file, or @-."),
			cli.String("endpoint-url", "").Help("HTTPS endpoint Mobius posts resolve requests to. Required for client_resolver."),
			cli.String("on-unavailable", "").Help("Behavior when the resolver endpoint is unreachable. Defaults to last_known_good."),
			cli.Int("protocol-version", "").Help("The resolve wire-protocol version Mobius speaks. Defaults to 1."),
			cli.Int("revalidate-after-s", "").Help("Skip the network when the cached bundle is younger than this (seconds)."),
			cli.String("source", "").Help("[required] Where the org's definitions resolve from by default."),
			cli.Int("stale-max-age-s", "").Help("Hard ceiling on serving last-known-good (seconds); 0 is unbounded."),
			cli.Int("timeout-ms", "").Help("Per-request resolve timeout in milliseconds. Defaults to 2000 when omitted or non-positive."),
			cli.String("file", "f").Help("Request body from a file (JSON or YAML, '-' for stdin). Flags override file contents."),
			cli.Bool("dry-run", "").Help("Print the assembled request body and exit without sending it."),
		).
		Use(requireAuth()).
		Run(func(ctx *cli.Context) error {
			mc, err := clientFromContext(ctx)
			if err != nil {
				return err
			}
			client := mc.RawClient()
			var body api.ReplaceDefinitionResolverJSONRequestBody
			if err := readJSONBody(ctx, &body); err != nil {
				return err
			}
			if ctx.IsSet("auth") {
				if err := decodeFlagJSON(ctx, "auth", ctx.String("auth"), &body.Auth); err != nil {
					return err
				}
			}
			if ctx.IsSet("endpoint-url") {
				v := ctx.String("endpoint-url")
				body.EndpointUrl = &v
			}
			if ctx.IsSet("on-unavailable") {
				v := api.PutDefinitionResolverRequestOnUnavailable(ctx.String("on-unavailable"))
				body.OnUnavailable = &v
			}
			if ctx.IsSet("protocol-version") {
				v := ctx.Int("protocol-version")
				body.ProtocolVersion = &v
			}
			if ctx.IsSet("revalidate-after-s") {
				v := ctx.Int("revalidate-after-s")
				body.RevalidateAfterS = &v
			}
			if ctx.IsSet("source") {
				body.Source = api.PutDefinitionResolverRequestSource(ctx.String("source"))
			}
			if ctx.IsSet("stale-max-age-s") {
				v := ctx.Int("stale-max-age-s")
				body.StaleMaxAgeS = &v
			}
			if ctx.IsSet("timeout-ms") {
				v := ctx.Int("timeout-ms")
				body.TimeoutMs = &v
			}
			if body.Source == "" {
				return fmt.Errorf("--source is required (or supply it via --file)")
			}
			if ctx.Bool("dry-run") {
				return printDryRun(ctx, body)
			}
			resp, err := client.ReplaceDefinitionResolverWithResponse(ctx.Context(), body)
			if err != nil {
				return err
			}
			return printResponse(ctx, "replaceDefinitionResolver", resp.StatusCode(), resp.Body)
		})

}
